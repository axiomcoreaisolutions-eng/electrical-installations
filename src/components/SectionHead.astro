---
interface Props {
  eyebrow: string;
  headline: string;
  supporting?: string;
  dark?: boolean;
  class?: string;
}

const { eyebrow, headline, supporting, dark = false, class: className } = Astro.props;
---

<div class:list={['max-w-3xl', className]} data-reveal>
  <p class:list={['eyebrow-chip', dark ? 'is-dark' : '']}>
    <span class="eyebrow-chip_dot" aria-hidden="true"></span>
    {eyebrow}
  </p>
  <h2
    data-reveal-words
    class:list={[
      'display mt-6 text-(length:--text-display-lg)',
      dark ? 'text-white' : 'text-ink',
    ]}
  >
    {headline}
  </h2>
  {
    supporting && (
      <p class:list={['mt-5 max-w-2xl text-lg leading-relaxed', dark ? 'text-mist' : 'text-slate']}>
        {supporting}
      </p>
    )
  }
</div>
