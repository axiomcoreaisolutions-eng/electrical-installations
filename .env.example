# Sanity — project ID and dataset are public identifiers, safe to expose
PUBLIC_SANITY_PROJECT_ID="4sqfr6zk"
PUBLIC_SANITY_DATASET="production"

# Optional: read token for private datasets / draft previews.
# NEVER commit a real token. Set it in Netlify/Cloudflare env settings.
SANITY_API_READ_TOKEN=""
